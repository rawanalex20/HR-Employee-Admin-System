<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <%= link_to t("Edit this user"), edit_user_path(@user) %> |
  <%= link_to t("Back to users"), users_path %>

  <%= button_to t("Destroy this user"), @user, method: :delete, class:"btn btn-primary"  %>
</div>
