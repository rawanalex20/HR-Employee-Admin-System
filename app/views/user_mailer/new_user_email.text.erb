<%= t '.Welcome', default: 'not found'%>, <%= @user.name %>!

<%= t '.User details', default: 'not found'%>
  --------------------------
  
  <%= t '.Name', default: 'not found'%>: <%= @user.name %>
  <%= t '.Email', default: 'not found'%>: <%= @user.email %>