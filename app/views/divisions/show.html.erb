<p style="color: green"><%= notice %></p>

<%= render @division %>

<div>
  <%= link_to t("Edit this division"), edit_division_path(@division) %> |
  <%= link_to t("Back to divisions"), divisions_path %>

  <%= button_to t("Destroy this division"), @division, method: :delete %>
</div>
